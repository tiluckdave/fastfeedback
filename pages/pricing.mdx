import DocsLayout from '@/components/DocsLayout';

export default (props) => <DocsLayout {...props} />;

# Pricing

Fast Feedback is still in alpha. It's free to sign up and leave feedback
and ₹49/month for unlimited sites and feedback.

Reach out to support@tiluckdave.in if you have any questions.
